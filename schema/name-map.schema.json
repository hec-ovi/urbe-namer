{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urbe/naming/name-map",
  "title": "ID-keyed name map: the naming pass output before patch-back",
  "type": "object",
  "required": ["names"],
  "properties": {
    "names": {
      "type": "object",
      "description": "placeholder entity id to chosen name; must cover every placeholder id exactly, no extra ids",
      "additionalProperties": { "type": "string", "minLength": 1 }
    }
  }
}
