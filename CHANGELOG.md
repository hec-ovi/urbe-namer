# Changelog

0.5.0: named worlds are validated by schema for structure and metadata and in code for selected-name coverage, so a 1,288 parcel city validates in milliseconds. The named copy is built and patched in one pass, and `blueprint.named.json` is written compact.

0.4.10: naming and NPC typing with validated parameters, repairable model output, streamed provider responses, business export and world-folder calls. Agent skill and isolated contract tests cover the public API.
